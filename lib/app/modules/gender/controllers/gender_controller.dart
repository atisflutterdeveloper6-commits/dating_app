import 'package:get/get.dart';
import '../../../routes/app_pages.dart';

class GenderController extends GetxController {
  // This controller is no longer needed for state management
  // but can be kept for navigation logic if needed
  
  void next(String gender, bool showGender) {
    // This method is now handled in the view with setState
    // You can remove this controller entirely if you want
  }
}